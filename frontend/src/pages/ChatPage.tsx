// Placeholder — real chat UI (message history, citations, context panel)
// lands with FE-003. FE-001's own scope is routing/layout only.
export function ChatPage() {
  return (
    <div className="flex h-full items-center justify-center text-muted-foreground">
      Chat — built in FE-003
    </div>
  );
}
