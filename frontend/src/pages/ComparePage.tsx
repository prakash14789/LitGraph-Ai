// Placeholder — real side-by-side GraphRAG-vs-vanilla comparison lands
// with COMPARE-001. FE-001's own scope is routing/layout only.
export function ComparePage() {
  return (
    <div className="flex h-full items-center justify-center text-muted-foreground">
      Compare — built in COMPARE-001
    </div>
  );
}
